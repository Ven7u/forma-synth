[package]
name = "forma-control"
version = "0.1.4"
license = "GPL-3.0-only"
edition = "2021"
publish = false

[dependencies]
crossbeam-channel = "0.5"
midir   = "0.11"
anyhow  = "1"
serde   = { version = "1", features = ["derive"], optional = true }

[features]
default = []
serde = ["dep:serde"]
